export enum tagTypes {
  flats = "flats",
  users = "users",
  workSpaces = "workSpaces",
  shopSpaces = "shopSpaces",
  myList = "myList",
}

export const tagTypesList = [
  tagTypes.users,
  tagTypes.flats,
  tagTypes.workSpaces,
  tagTypes.shopSpaces,
  tagTypes.myList,
];
