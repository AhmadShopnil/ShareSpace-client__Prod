export enum tagTypes {
  flats = "flats",
  users = "users",
  workSpaces = "workSpaces",
  myList = "myList",
}

export const tagTypesList = [
  tagTypes.users,
  tagTypes.flats,
  tagTypes.workSpaces,
  tagTypes.myList,
];
